/**
 * Default token limits for models
 */
export const DEFAULT_MAX_INPUT_TOKENS = 128000;
export const DEFAULT_MAX_OUTPUT_TOKENS = 64000;

/**
 * Whether the Manage Providers panel lists recently-added providers first.
 */
export const DEFAULT_PROVIDER_LIST_NEWEST_FIRST = true;
